<% unless user_signed_in? %>
    <div class="hero-unit">
        <h1>Welcome to Image Uploader</h1>
        <p>
            Display your favourite images here.
        </p>
        <p>
            <%= link_to "Sign Up Now!", new_user_registration_path, class: "btn btn-primary btn-large" %>
        </p>
    </div>
<% end %>